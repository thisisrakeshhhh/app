# Use the full JetBrains JDK bundled with Android Studio.
# A version-only match can select VS Code's trimmed Java runtime without jlink.
# Install Android Studio's JBR 21 locally; no machine-specific path is committed.
toolchainVersion=21
toolchainVendor=JETBRAINS
